/*introduced in java8.0 version.
 * lambda functions are anonymous functions. A function which does not have any name is anonymous function.
 * lambda function is an implementation of functional interface.*/
package lambda;
/*an interface with one abstract method is called functional interface.
 * functional interface is also called SAM(single abstract method) interface.
 * @FunctionalInterface is a ANNOTATION which is used to declare interface as a functional interface. annotation is a special type which provide information to the compiler(or)JVM.
 * {only one (@)annotation is used & only one abstract method. functional interface.}*/
public @interface FuncationalInterface {

}
